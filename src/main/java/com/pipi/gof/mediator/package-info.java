package com.pipi.gof.mediator;

/**
 中介者模式 MediatorImpl
    用一个中介对象来封装一系列的对象交互，中介者使各对象不需要显式地相互引用，从而使其耦合松散，而且可以独立地改变它们之间的交互。
    如果一个系统中对对象之间的联系呈现网状结构，对象之间存在大量多对多关系，将导致关系及其复杂，这些对象称为“同事对象”。
    引入一个中介者对象，是各个同事对象只跟中介者对象打交道，将其复杂的网络结构化解为如下的形状结构。
 */
